# Resend email service API key
# Get one at https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Email address(es) that receive demo-request notifications
# (comma-separated if multiple)
DEMO_REQUEST_NOTIFY_EMAIL=your-email@example.com

# Email address(es) that receive contact-form submissions
CONTACT_NOTIFY_EMAIL=your-email@example.com